[pytest]
markers =
    unit: fast isolated checks for pure logic and local contracts
    integration: checks that exercise real in-process app paths and persistence boundaries
    e2e: full stack checks that require external services or containers
    prod_readonly: read-only checks that are safe against production
