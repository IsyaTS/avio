# Sample environment configuration
# Replace with your own secret before running the application
# Public access token for Telegram QR/status endpoints (falls back to ADMIN_TOKEN when empty)
PUBLIC_KEY=replace-with-your-public-key
